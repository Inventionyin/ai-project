下面给你一版 仅方案、不改代码 的详细落地稿。
 并且按你最新要求把 AI 导入源统一为： PRD 文档 + Figma 链接 + HTML 文档 。

你现有基础我按这些文件做了对齐：

- 后端用例域： testcases.py
- 后端模型： testcase.py
- 前端用例页： CasesPanel.vue
- 前端用例表： CasesTable.vue
- 前端报告页： ReportsPanel.vue
一、AI导入类型（调整后）

- PRD_DOC ：支持 .md/.txt/.docx/.pdf （建议首期先 md/txt/docx ，pdf作为P1）
- FIGMA_LINK ：输入 Figma URL（fileKey + nodeId）
- HTML_DOC ：支持 .html/.htm
建议 sourceType 枚举最终定为：

- PRD_DOC | FIGMA_LINK | HTML_DOC
二、接口请求/响应 JSON 示例

1) 创建AI导入任务

- POST /api/testcases/ai-import/jobs
请求：

```
{
  "projectId": 
  "2f1e8b15-5a0f-4df1-9f85-e7a8ed47a001",
  "sourceType": "FIGMA_LINK",
  "source": {
    "figmaUrl": "https://www.figma.com/file/
    AbCdEf123456/Checkout?node-id=100-200"
  },
  "generateConfig": {
    "language": "zh-CN",
    "maxCases": 80,
    "dedupeStrategy": "TITLE_STORY_URL",
    "defaultPriority": "P1",
    "defaultType": "API",
    "autoExtractApi": true
  },
  "skillConfig": {
    "enableFeature": true,
    "enableEpic": true,
    "enableStory": true,
    "enableTask": true,
    "enableDescription": true,
    "enableTitle": true,
    "enableStep": true
  }
}
```
响应：

```
{
  "code": 0,
  "message": "ok",
  "data": {
    "jobId": 
    "e8e2c997-7267-4e59-a80f-2f3c10b3a111",
    "status": "PENDING",
    "sourceType": "FIGMA_LINK",
    "createdAt": 1773889000
  },
  "requestId": "req_001"
}
```
2) 上传文件源（PRD/HTML）

- POST /api/testcases/ai-import/jobs/{jobId}/file
- Content-Type: multipart/form-data
- 表单字段： file 、 filename 、 sourceType （PRD_DOC/HTML_DOC）
响应（JSON）：

```
{
  "code": 0,
  "message": "ok",
  "data": {
    "jobId": 
    "e8e2c997-7267-4e59-a80f-2f3c10b3a111",
    "status": "UPLOADED",
    "storageKey": "ai-import/2026/03/19/
    e8e2c997/source.docx"
  },
  "requestId": "req_002"
}
```
3) 启动解析与生成

- POST /api/testcases/ai-import/jobs/{jobId}/start
请求：

```
{
  "parseConfig": {
    "splitByHeading": true,
    "extractTables": true,
    "extractFlows": true
  }
}
```
响应：

```
{
  "code": 0,
  "message": "ok",
  "data": {
    "jobId": 
    "e8e2c997-7267-4e59-a80f-2f3c10b3a111",
    "status": "RUNNING"
  },
  "requestId": "req_003"
}
```
4) 查询任务状态与预览

- GET /api/testcases/ai-import/jobs/{jobId}
响应：

```
{
  "code": 0,
  "message": "ok",
  "data": {
    "jobId": 
    "e8e2c997-7267-4e59-a80f-2f3c10b3a111",
    "projectId": 
    "2f1e8b15-5a0f-4df1-9f85-e7a8ed47a001",
    "status": "SUCCEEDED",
    "progress": 100,
    "summary": {
      "rawBlocks": 42,
      "generatedCases": 26,
      "dedupedCases": 23,
      "lowConfidenceCases": 4
    },
    "previewItems": [
      {
        "itemId": 
        "f497f852-8a25-4da3-b1ec-79ac96f01a11"
        ,
        "title": "支付成功页应展示订单编号",
        "feature": "结算中心",
        "epic": "支付流程",
        "story": "用户完成支付后查看结果",
        "task": "验证支付成功落地页",
        "description": "支付完成后系统展示关键信息
        ",
        "steps": ["创建订单", "发起支付", "校验成
        功页字段"],
        "apiUrl": "/api/payments/{id}",
        "apiMethod": "GET",
        "priority": "P1",
        "type": "API",
        "confidence": 0.87
      }
    ],
    "errorMessage": null
  },
  "requestId": "req_004"
}
```
5) 选择性入库（提交生成结果）

- POST /api/testcases/ai-import/jobs/{jobId}/commit
请求：

```
{
  "selectedItemIds": [
    "f497f852-8a25-4da3-b1ec-79ac96f01a11",
    "f497f852-8a25-4da3-b1ec-79ac96f01a12"
  ],
  "saveAsStatus": "DRAFT",
  "ownerId": 
  "8cfbe836-dbd8-411f-9c60-66fbd9f53a01"
}
```
响应：

```
{
  "code": 0,
  "message": "ok",
  "data": {
    "jobId": 
    "e8e2c997-7267-4e59-a80f-2f3c10b3a111",
    "inserted": 2,
    "skipped": 0,
    "testcaseIds": [
      "53ac938e-223f-4bdf-8c93-c47f53dbf001",
      "53ac938e-223f-4bdf-8c93-c47f53dbf002"
    ]
  },
  "requestId": "req_005"
}
```
6) 用例列表返回新增字段（兼容扩展）

- GET /api/testcases?...
data.items[] 增加：

```
{
  "id": 
  "53ac938e-223f-4bdf-8c93-c47f53dbf001",
  "title": "支付成功页应展示订单编号",
  "type": "API",
  "priority": "P1",
  "status": "DRAFT",
  "feature": "结算中心",
  "story": "用户完成支付后查看结果",
  "apiUrl": "/api/payments/{id}",
  "apiMethod": "GET",
  "lastRun": "PASSED",
  "updatedAt": 1773889200
}
```
三、表结构 DDL 草案（PostgreSQL）

1) 扩展 testcases 表

```
ALTER TABLE testcases
  ADD COLUMN feature VARCHAR(128),
  ADD COLUMN story VARCHAR(128),
  ADD COLUMN api_url VARCHAR(1024),
  ADD COLUMN api_method VARCHAR(16),
  ADD COLUMN ai_meta_json JSONB NOT NULL 
  DEFAULT '{}'::jsonb,
  ADD COLUMN generated_by_ai BOOLEAN NOT NULL 
  DEFAULT FALSE;

CREATE INDEX ix_testcases_project_feature ON 
testcases(project_id, feature);
CREATE INDEX ix_testcases_project_story ON 
testcases(project_id, story);
CREATE INDEX ix_testcases_project_api_url ON 
testcases(project_id, api_url);
```
2) AI导入任务表

```
CREATE TABLE ai_import_jobs (
  id UUID PRIMARY KEY,
  tenant_id UUID NOT NULL REFERENCES tenants
  (id),
  project_id UUID NOT NULL REFERENCES projects
  (id),
  source_type VARCHAR(32) NOT NULL CHECK 
  (source_type IN ('PRD_DOC', 'FIGMA_LINK', 
  'HTML_DOC')),
  status VARCHAR(32) NOT NULL CHECK (status 
  IN ('PENDING', 'UPLOADED', 'RUNNING', 
  'SUCCEEDED', 'FAILED', 'COMMITTED')),
  source_ref_json JSONB NOT NULL DEFAULT '{}
  '::jsonb,
  generate_config_json JSONB NOT NULL DEFAULT 
  '{}'::jsonb,
  skill_config_json JSONB NOT NULL DEFAULT '{}
  '::jsonb,
  summary_json JSONB NOT NULL DEFAULT '{}
  '::jsonb,
  error_message TEXT,
  created_by UUID REFERENCES users(id),
  created_at TIMESTAMP NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX 
ix_ai_import_jobs_tenant_project_created
  ON ai_import_jobs(tenant_id, project_id, 
  created_at DESC);
```
3) AI导入明细表（预览项）

```
CREATE TABLE ai_import_items (
  id UUID PRIMARY KEY,
  tenant_id UUID NOT NULL REFERENCES tenants
  (id),
  job_id UUID NOT NULL REFERENCES 
  ai_import_jobs(id) ON DELETE CASCADE,
  project_id UUID NOT NULL REFERENCES projects
  (id),
  title VARCHAR(100) NOT NULL,
  type VARCHAR(16) NOT NULL,
  priority VARCHAR(8) NOT NULL,
  status VARCHAR(16) NOT NULL,
  feature VARCHAR(128),
  epic VARCHAR(128),
  story VARCHAR(128),
  task VARCHAR(128),
  description TEXT,
  steps_json JSONB NOT NULL DEFAULT '[]
  '::jsonb,
  api_url VARCHAR(1024),
  api_method VARCHAR(16),
  tags_json JSONB NOT NULL DEFAULT '[]
  '::jsonb,
  ai_meta_json JSONB NOT NULL DEFAULT '{}
  '::jsonb,
  confidence NUMERIC(5,4),
  dedupe_key VARCHAR(256),
  selected BOOLEAN NOT NULL DEFAULT TRUE,
  created_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE INDEX ix_ai_import_items_job ON 
ai_import_items(job_id);
CREATE INDEX ix_ai_import_items_project_story 
ON ai_import_items(project_id, story);
```
4) 报告主表（Allure索引）

```
CREATE TABLE reports (
  id UUID PRIMARY KEY,
  tenant_id UUID NOT NULL REFERENCES tenants
  (id),
  project_id UUID NOT NULL REFERENCES projects
  (id),
  run_id UUID NOT NULL REFERENCES runs(id),
  report_type VARCHAR(32) NOT NULL CHECK 
  (report_type IN ('ALLURE')),
  status VARCHAR(32) NOT NULL CHECK (status 
  IN ('GENERATING', 'READY', 'FAILED')),
  summary_json JSONB NOT NULL DEFAULT '{}
  '::jsonb,
  allure_result_key VARCHAR(1024),
  allure_report_url VARCHAR(2048),
  generated_at TIMESTAMP,
  created_at TIMESTAMP NOT NULL DEFAULT NOW(),
  updated_at TIMESTAMP NOT NULL DEFAULT NOW()
);

CREATE UNIQUE INDEX uq_reports_run_id ON 
reports(run_id);
CREATE INDEX ix_reports_project_created ON 
reports(project_id, created_at DESC);
```
四、前端页面字段清单（逐组件）

A. CasesPanel.vue

- 顶部新增按钮： AI导入
- AI导入弹窗字段：
  - sourceType （PRD文档 / Figma链接 / HTML文档）
  - figmaUrl （sourceType=FIGMA_LINK 时必填）
  - file （sourceType=PRD_DOC/HTML_DOC 时必填）
  - maxCases
  - dedupeStrategy
  - defaultPriority
  - defaultType
  - skillToggles （feature/epic/story/task/description/title/step）
- 任务进度区字段：
  - jobId
  - status
  - progress
  - generatedCases
  - dedupedCases
  - lowConfidenceCases
  - errorMessage
- 入库提交字段：
  - selectedItemIds[]
  - saveAsStatus
  - ownerId
  B. CasesTable.vue

- 行数据新增字段：
  - feature
  - story
  - apiUrl
  - apiMethod
- 交互字段：
  - checked
  - selectedCount
  - isAllCheckedCurrentPage
- 表头新增列：
  - 调用URL
  - Story
  - Feature
  C. 新增组件建议：AiImportModal.vue（新建）

- 基础字段： visible 、 sourceType 、 figmaUrl 、 file
- 配置字段： maxCases 、 defaultPriority 、 defaultType 、 dedupeStrategy
- 技能字段： enableFeature/enableEpic/enableStory/enableTask/enableDescription/enableTitle/enableStep
- 状态字段： jobId 、 status 、 progress 、 errorMessage
D. 新增组件建议：AiImportPreviewTable.vue（新建）

- 列字段：
  - select
  - title
  - feature
  - epic
  - story
  - task
  - apiUrl
  - type
  - priority
  - confidence
- 操作字段：
  - include/exclude
  - editBeforeCommit （可选）
  E. AiTestingPlatformShell.vue

- 执行弹窗（已有）扩展字段：
  - runMode （SUITE / TESTCASES）
  - testcaseIds[] （runMode=TESTCASES）
  - envId
  - triggerType
  - idempotencyKey （自动生成）
  - notifyOnFail
  F. ReportsPanel.vue

- 列表筛选字段：
  - projectId
  - runId
  - status
  - timeRange
- 列表项字段：
  - reportId
  - runId
  - passRate
  - total/passed/failed/skipped
  - generatedAt
  - reportStatus
- 详情字段：
  - featureStats[]
  - storyStats[]
  - failedCases[]
  - allureReportUrl
  五、落地建议（最小可交付顺序）

- 第1步：完成 ai_import_jobs/items + 任务接口 + PRD/FIGMA/HTML三种入口。
- 第2步：完成 preview + commit 入库（写入 testcases 扩展字段）。
- 第3步：CasesTable 展示新列 + 勾选批量执行。
- 第4步：reports 表 + 报告中心联调（先列表后详情）。
如果你认可，我下一条可以直接给你：

- OpenAPI 风格接口定义稿 （可直接给后端）
- Alembic 迁移脚本模板 （按你现有版本号规范）
- 前端 TypeScript 类型定义清单 （可直接落到 types 文件