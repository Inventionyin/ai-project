1.启动bat文件无法直接启动，- 前端依赖未完整安装，导致 vite /路由相关包缺失，启动中断。
- 后端在 Windows 下存在导入路径冲突：项目里的 alembic 目录会和 Alembic 包名冲突，且 app 包路径在子进程里未正确解析。
已修改

解决：

- 优化启动脚本，增加环境与依赖自检、自动补装、并修正后端启动方式：

- start-dev.bat

- 补齐前端缺失依赖：

- package.json

- 增强后端入口导入兼容（避免不同启动路径下直接报错）：

- main.py

  2.bat文件不会主动启动与数据库的连接

  3.数据库需本地部署
  4.非管理员可以查看其他项目（无权限）